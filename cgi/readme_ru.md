# CGI скрипт проекта Ile

## Базовая информация

* Для сборки используются `cmake` и `make`

## Как собирать

1. Собрать статическую библиотеку в `CMake`
2. Собрать CGI-скрипт через `makefile` (он ссылается на стат. библиотеку)
